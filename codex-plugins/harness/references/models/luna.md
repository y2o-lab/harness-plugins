# GPT-5.6 Luna

For clear, repeatable tasks, make the input, expected output, target files, and acceptance check explicit. Use a representative existing example when it clarifies the contract. Work in bounded units, inspect the result, and keep unrelated repository context out of tool output.

Use scripts for mechanical transformations and validators for observable correctness. When requirements conflict or an attempt fails, gather the relevant error and revise the approach. Report a specific blocker if the task cannot be resolved with available evidence; do not silently reduce quality requirements or switch models.
