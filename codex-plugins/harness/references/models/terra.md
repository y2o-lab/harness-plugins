# GPT-5.6 Terra

For everyday implementation, identify the affected interface, repository conventions, and observable result. Divide a larger change into coherent steps with a relevant check at each meaningful boundary, without forcing a plan for a small edit.

Keep context focused on touched code and its callers. Use deterministic tools for inspection and verification. If uncertainty persists after a focused check, identify the specific missing evidence instead of repeating the same attempt or silently changing models.
